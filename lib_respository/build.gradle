apply from: "../basic.gradle"

dependencies {
    lifecycleDependencies.each { k, v -> implementation v }
    networkDependencies.each { k, v -> api v }
    compileOnly projectModule.lib_base
    compileOnly projectModule.lib_utils
}